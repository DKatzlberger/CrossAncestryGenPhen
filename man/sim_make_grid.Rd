% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_make_grid.R
\name{sim_make_grid}
\alias{sim_make_grid}
\title{Build a parameter grid}
\usage{
sim_make_grid(..., zero_rules = TRUE, verbose = TRUE)
}
\arguments{
\item{...}{Named vectors of parameters.}

\item{zero_rules}{Logical, default `TRUE`. Apply `n_degs`/`log2fc` zeroing rule.}

\item{verbose}{Logical, default `TRUE`. Print the grid and number of rows.}
}
\value{
A data.frame of parameter combinations.
}
\description{
Creates a grid of parameter combinations with `expand.grid()`.  
Optionally enforces a rule for `n_degs` and `log2fc` (set both to 0 if either is 0),  
and prints the grid when `verbose = TRUE`.
}
\examples{
sim_make_grid(n_samples = c(10), n_degs = c(0,1000), log2fc = c(0,1))
sim_make_grid(total_samples = 1000, between_ratio = 2, within_ratio = 2, zero_rules = FALSE)

}
