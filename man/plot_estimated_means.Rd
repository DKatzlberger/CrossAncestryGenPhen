% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_estimated_means.R
\name{plot_estimated_means}
\alias{plot_estimated_means}
\title{Plot Estimated Gene Means Between Two Datasets}
\usage{
plot_estimated_means(
  estimates_X,
  estimates_Y,
  method = c("raw", "logcpm", "mle", "map", "libnorm_mle", "libnorm_map"),
  log = TRUE,
  ancestry_X = "Dataset X",
  ancestry_Y = "Dataset Y",
  title = NULL,
  x_label = NULL,
  y_label = NULL,
  point_size = 1
)
}
\arguments{
\item{estimates_X}{A list returned by `estimate_params()` for dataset X. Must contain a `means` sublist.}

\item{estimates_Y}{A list returned by `estimate_params()` for dataset Y. Must contain a `means` sublist.}

\item{method}{Character. Type of gene-level mean to compare. One of `"raw"` (unadjusted counts),
`"libnorm"` (library-normalized counts), or `"logcpm"` (log2 counts per million).}

\item{log}{Logical. Whether to log10-transform the mean values before plotting. Default is `FALSE`.}

\item{ancestry_X}{Character. Label used for dataset X in axis titles. Default is `"Dataset X"`.}

\item{ancestry_Y}{Character. Label used for dataset Y in axis titles. Default is `"Dataset Y"`.}

\item{title}{Plot title.}

\item{x_label}{X-axis label.}

\item{y_label}{Y-axis label.}

\item{point_size}{Numeric. Size of the scatter plot points. Default is `1`.}
}
\value{
A [ggplot2::ggplot()] object showing gene-wise mean comparison.
}
\description{
This function generates a scatter plot comparing gene-wise expression means
between two datasets (`estimates_X` and `estimates_Y`) for a specified normalization method.
A reference line with slope = 1 is included to help visualize agreement between datasets.
}
\examples{
\dontrun{
estimates_X <- estimate_params(count_matrix_X)
estimates_Y <- estimate_params(count_matrix_Y)
plot_estimated_means(estimates_X, estimates_Y, method = "libnorm", log = TRUE)
}

}
