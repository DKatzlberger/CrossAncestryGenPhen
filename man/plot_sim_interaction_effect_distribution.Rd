% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_sim_interaction_effect_distribution.R
\name{plot_sim_interaction_effect_distribution}
\alias{plot_sim_interaction_effect_distribution}
\title{Plot Interaction Effect Distribution with Optional Split}
\usage{
plot_sim_interaction_effect_distribution(
  data,
  x_var,
  effect_thr = NULL,
  title = NULL,
  x_label = NULL,
  y_label = NULL,
  bins = 50
)
}
\arguments{
\item{data}{A `data.frame` or similar object containing the variable to plot.}

\item{x_var}{A string. The name of the column in `data` to plot on the x-axis.}

\item{effect_thr}{Numeric. Threshold for splitting the distribution 
(symmetric around zero). If `NULL`, no split is done and a single 
histogram is plotted.}

\item{title}{Optional plot title.}

\item{x_label}{Optional x-axis label for the middle panel.}

\item{y_label}{Optional y-axis label for the left panel.}

\item{bins}{Number of bins for the histogram.}

\item{fill_label}{Optional fill legend label (not used by default).}
}
\value{
A `ggplot` object or a `{patchwork}` combined plot if 
  `effect_thr` is provided.
}
\description{
This function plots the distribution of an interaction effect variable 
using `ggplot2`. If `effect_thr` is provided, the distribution is split 
into three symmetric panels: 
- Below region: values less than `-effect_thr`
- Inside region: values between `-effect_thr` and `+effect_thr`
- Above region: values greater than `+effect_thr`
}
\details{
Panels are combined side-by-side using `{patchwork}`. Each panel shows 
symmetric x-axis tick marks and optional axis labels.
}
